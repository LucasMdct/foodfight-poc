// Task 10 implements the actual Game Over modal (final score, best, Play
// Again). Stubbed here so GameScreen can render it unconditionally on
// screen === 'over' without depending on unfinished work.
export const GameOverModal = () => null;

export default GameOverModal;
